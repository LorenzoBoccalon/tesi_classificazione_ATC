In questa cartella vengono salvati i risultati dell'utilizzo del pacchetto MatLearn per classificare

	-classMatLearn.mat: contiene tutte le variabili di ambiente di classificatoreMatLearn.m
 	-classLSTMatLearn.mat: contiene tutte le variabili di ambiente di classificatoreLSTMatLearn.m

	-classificatoreLSTMatLearn.m: codice che utilizza pacchetto matLearn assieme a LSTM
	-classificatoreMatLearn.m: codice che utilizza pacchetto matLearn senza LSTM

	-performance.txt: contiene i per.val. di riferimento