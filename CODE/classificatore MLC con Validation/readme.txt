All'interno della cartella vengono salvate le modifiche e i risultati di Validation
che si occupa di utilizzare una parte del training set come validation set per LSTM
	
	-MainLSTMmodVal.m: codice modificato per usare il valset
	-ParametriLSTMmodVal.m: param. LSTM modificati per usare il valset

	-validation.mat: variabili di ambiente 

	-performance.txt: per. val. con valset